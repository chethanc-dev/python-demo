name: Python CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Check Python Version
        run: python3 --version

      - name: Run Python Application
        run: python3 app.py
